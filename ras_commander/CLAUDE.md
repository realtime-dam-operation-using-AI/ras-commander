@AGENTS.md

## Claude Adapter Notes

- Claude may auto-load relevant `.claude/rules/` files while working in `ras_commander/`.
- Shared library behavior still belongs in `ras_commander/AGENTS.md` and deeper subpackage `AGENTS.md` files.
