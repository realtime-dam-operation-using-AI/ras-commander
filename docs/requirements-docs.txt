# Documentation build dependencies
# Install with: pip install -r docs/requirements-docs.txt

# MkDocs core
mkdocs>=1.5.0

# Material theme
mkdocs-material>=9.5.0

# API documentation from docstrings
mkdocstrings[python]>=0.24.0

# Git revision dates
mkdocs-git-revision-date-localized-plugin>=1.2.0

# For cognitive infrastructure doc generation
PyYAML>=6.0

# For notebook pre-conversion to markdown (30x faster than mkdocs-jupyter)
nbconvert>=7.0.0
jupyter>=1.0.0

# Package dependencies for mkdocstrings to import modules
# (excludes pywin32 which is Windows-only)
h5py
numpy
pandas
requests
tqdm
scipy
xarray
geopandas
matplotlib
shapely
rasterstats
rtree
psutil>=5.6.6

# Interactive Leaflet maps embedded in example-notebook outputs. folium emits a
# self-contained <iframe srcdoc> that survives nbconvert -> MkDocs (ipyleaflet
# widgets do NOT survive the static build); branca provides the colormap legend.
folium>=0.15
branca>=0.7
